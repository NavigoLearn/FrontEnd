---
import Base from '@layouts/Base.astro';
import Profile from '../../components/profile/Profile';
import { parse } from 'path';

const { id } = Astro.params;
---

<Base
  metadata={{
    title: 'Profile',
  }}
>
  <div>
    <Profile id={id} client:load />
  </div>
</Base>
